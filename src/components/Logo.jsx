import React from "react";
import logo from "../assests/logo.png";

const Logo = () => {
  return (
    <div>
      <img src={logo} width={80} height={100} />
    </div>
  );
};

export default Logo;
